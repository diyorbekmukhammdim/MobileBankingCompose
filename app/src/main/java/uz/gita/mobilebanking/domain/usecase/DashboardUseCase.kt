package uz.gita.mobilebanking.domain.usecase

interface DashboardUseCase {
    /*
    fun getBooks(): Flow<Result<List<CardData>>>
    fun add(card: CardData): Flow<Result<CardData>>
    fun delete(card: CardData): Flow<String>
    fun update(card: CardData): Flow<String>

    fun changeFavourite(card: CardData): Flow<String>
    fun sync(viewModelScope: CoroutineScope)

    suspend fun logout()
    */
}
